# Railway config for SaaS API service
# Set in Railway dashboard: Settings > Config File Path = railway.saas.toml

[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile.saas"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 100
restartPolicyType = "on_failure"
